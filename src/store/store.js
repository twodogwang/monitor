import Vue from "vue";
import Vuex from "vuex";
import routeHistory from "./modules/routeHistory";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    routeHistory
  }
});
